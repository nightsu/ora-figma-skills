# §4c — `figma-emit-spec` 契约(新建)

> 上一篇:[04b Design Token](./04b-design-token.md) · 下一篇:[05 Review Gate & Handoff](./05-review-gate-and-handoff.md)

---

## 职责

合并 A/B/C/D 所有产物,产出 `implementation-spec.md`(Agent 编码主输入)+
`open-questions.md`(跨阶段问题汇总),并在出口处提供 handoff 选择。

---

## 前置条件

A + B + C(上 + 下) + D 全部 5 份产物存在且非占位:
- `clarified-requirement.md`
- `ui-understanding.md`
- `api-mapping.md`
- `component-mapping.md`
- `design-token-patch.md`

---

## 输入(本阶段收集)

无新增输入(纯合成 skill)。

---

## 读取(上游产物)

| 文件 | 用途 |
|---|---|
| `clarified-requirement.md` | Goal / Scope / User States / Interaction |
| `ui-understanding.md` | Modules / Suspected Components |
| `api-mapping.md` | API → UI 字段映射 |
| `component-mapping.md` | 节点分类 + UI↔API 字段对照 |
| `design-token-patch.md` | 视觉 token + 资源引用 |

---

## 调用外部能力

**无**。
- 不调用 Figma MCP
- 不联网
- 不调用 superpowers / openspec 等外部 skill(handoff 是用户选择,见 §5)

---

## 产物

| 路径 | 类型 | 说明 |
|---|---|---|
| `docs/design/<feature>/implementation-spec.md` | 主产物 | Agent apply 阶段唯一主输入 |
| `docs/design/<feature>/open-questions.md` | 主产物 | 跨阶段未决问题汇总 |
| `docs/design/<feature>/inputs.md` | 追加一条 | 含 inputs read 列表 + handoff choice |
| `docs/design/<feature>/.workflow-prefs.json` | (handoff 首次选择时)创建 | 记 handoff 选择 |
| `docs/design/<feature>/task-breakdown.md` | (handoff = builtin) | 从 spec 拆出的 task 清单 |

> handoff 走 superpowers / manual / pause 时,不产出 `task-breakdown.md`。
> handoff 详细见 [§5](./05-review-gate-and-handoff.md)。

---

## 产物格式(`implementation-spec.md`)

```markdown
# Implementation Spec — <feature>

> Source of truth for Agent apply stage. DO NOT consume raw Figma JSON in apply.
> Generated by figma-emit-spec@0.1.0 at <ISO8601>

## Page
<PageName, e.g. ReferralHomePage>

## Goal
(从 clarified-requirement.md 的 ## Goal 节抽取,原文照搬)

## Scope / Out of Scope
(从 clarified-requirement.md 抽取,原文照搬)

## Modules

### 1. DiamondPreviewCard

**Responsibilities** (从 ui-understanding.md 推断 + clarified-requirement 校准)
- 展示用户当前钻石余额
- 提供钻石记录入口

**Data Binding** (从 api-mapping.md + component-mapping.md 合并)
| UI Field | API Field | Transform |
|---|---|---|
| diamond_value | user.diamond.balance | formatNumber |
| record_entry | — (固定文案) | — |

**Component** (从 component-mapping.md 推断)
- 优先复用项目已有 `DiamondCard`(若存在)
- 否则新建 `components/DiamondPreviewCard/`

**Design Tokens** (从 design-token-patch.md 抽取)
- borderRadius: 12px
- padding: 12px 16px
- titleFontSize: 14px
- valueFontSize: 24px
- valueColor: #1A1A1A (Figma variable: color/brand/primary)

**States**
- loading / empty / error(从 clarified-requirement 的 User States 推断)

### 2. OperationEntryGrid
... (省略类似格式)

## API Usage
| Module | API | Suggested Hook |
|---|---|---|
| DiamondPreviewCard | GET /api/diamond/overview | useDiamondOverview |

## Interaction
(从 clarified-requirement.md 的 Interaction 节抽取,原文照搬)

## Implementation Constraints
- 不直接复刻 Figma 图层层级
- 不将装饰性节点实现为独立组件
- 重复列表项使用数据驱动渲染
- 优先复用项目已有组件 / tailwind / css_module
- 样式 token 来自 design-token-patch.md
- API 字段以 api-mapping.md 为准

## Verification Checklist
- [ ] 与 Figma 截图视觉对比(允许文本不严格一致)
- [ ] 接口字段类型与 API 文档一致
- [ ] 复用项目已有组件 / 框架(tailwind / css_module)
- [ ] 所有 open questions 已解决或显式 defer

## Open Questions
见 open-questions.md
```

---

## 产物格式(`open-questions.md`)

```markdown
# Open Questions — <feature>

聚合自所有阶段产物中标记的待确认项。

## From Phase A (clarified-requirement.md)
- [ ] 入口数量是否固定?
- [ ] 列表是否分页?

## From Phase B (ui-understanding.md)
- [ ] 空状态是否有单独设计?

## From Phase C (component-mapping.md, needs_confirmation: true)
- [ ] node `cover_image_123`: 来源不明(banner 接口 / 固定素材)
- [ ] node `tag_456`: 标签文案可能来自接口

## From Phase D (design-token-patch.md, INFERRED)
- [ ] OperationEntryGrid.columnCount: INFERRED from 4-instance grid,需确认是否固定

## Cross-Product Conflicts (auto-detected)
- [ ] `api-mapping.md` 声明字段 `user.diamond.history`,但 `component-mapping.md` 未绑定到任何 UI node
```

---

## 关键行为

1. **合成时不引入新事实**,只重组上游产物的事实
2. 任何新增/推断的内容必须显式标 `(INFERRED)`,或写入 `open-questions.md`
3. 自检 open-questions 数量,数量 > 0 时在 review gate 明确提示
   "建议先回答 open questions 再 handoff"
4. handoff 出口详细行为见 §5
5. 不修改上游产物
6. 跨产物冲突检测见下方"Cross-product conflicts 检测"

---

## Cross-product conflicts 检测(MVP 最小集)

| 冲突类型 | 检测方式 | 处理方式 |
|---|---|---|
| `field_unbound`: `api-mapping.md` 中的字段未在 `component-mapping.md` 中被绑定 | 字段名字符串匹配 | 写入 open-questions |
| `module_missing_token`: `component-mapping.md` 中的 module 未在 `design-token-patch.md` 中抽 token | module 名字符串匹配 | 写入 open-questions |
| `module_drift`: `ui-understanding.md` 中的 module 未在 `component-mapping.md` 中出现 | module 名字符串匹配 | 写入 open-questions |
| **`label_drift`: `component-mapping.md` 中槽位 label 与 `design-token-patch.md` 真实 label 不一致** | label 字符串匹配 | **自动以 `design-token-patch.md` 为准,在 implementation-spec metadata 段说明"N 个 label 已自动校正"**,**不写入 open-questions**(答案已知,不需要人介入) |

冲突一律写入 `open-questions.md` 的 `## Cross-Product Conflicts (auto-detected)` 段,
在 review gate 标红提示。**例外:** `label_drift` 不写入 open-questions,而是在 implementation-spec metadata 段说明自动校正情况。

> ⚠ MVP 检测是粗粒度字符串匹配,可能 false positive。
> 用户可以直接在 `open-questions.md` 中标 `[x]` 表示已确认非冲突,
> orchestrator 不重写该段。

### 为什么 `label_drift` 自动校正而非写入 open-questions

`figma-ui-api-mapper`(phase C2)可能未拿到真实 `characters`(若 Figma 文本节点 name 是 placeholder,
当 spec §4a 工作流要求同时调 `get_design_context` 后此情况大幅减少,但仍存在 edge case)。

`figma-design-token`(phase D)通过 `get_design_context` 拿到的 `characters` 是真实业务文案的事实来源。
冲突时以 D 为准,不需要人介入。这是 **MVP validation 发现的真实场景**(见 [validation-findings.md §F2](./validation-findings.md))。

---

## Deferred open questions(无解决路径的 open question 处理)

某些 open questions 在本期无法解决(例:接口缺字段、设计 vs 后端不对齐),
不应反复阻塞 emit-spec。引入 `[deferred]` 前缀机制:

**机制:**
- 用户在 `open-questions.md` 中,对无法解决的项加 `[deferred]` 前缀,
  例如: `- [ ] [deferred] 趋势数据来源:接口完全无字段,本期不实现 trend badge`
- `figma-emit-spec` 在合成 `implementation-spec.md` 时识别 `[deferred]`:
  - 对应 UI 元素在 Modules 段**显式标记**:`> ⚠ Deferred: <原 open question 内容>。本期不实现,占位渲染或隐藏。`
  - 不再视为 blocker
- Verification Checklist 增加一项:`[ ] 所有 deferred items 已确认本期不实现`

**例子:**

```markdown
### N. PrimaryMetricCard - 趋势 Badge

> ⚠ Deferred: 接口完全无趋势字段。本期 trend badge 占位渲染或隐藏。
> 来源:phase A open-questions.md #4

(本元素不参与本期实施)
```

---

## 不做的事

- ❌ 不调用 Figma MCP
- ❌ 不联网
- ❌ 在 spec 里**绝对不引用** raw Figma JSON
- ❌ 不修改上游产物
- ❌ 不自动答 open questions
- ❌ 不在 handoff = manual / pause 时产出 `task-breakdown.md`

---

## 错误处理

| 情况 | 处理 |
|---|---|
| 任一上游产物缺失 | orchestrator 已拦截 |
| 上游产物互相冲突 | 写入 open-questions,review gate 标红;**不阻塞**产物落盘 |
| 上游产物 markdown 解析失败(格式严重崩坏) | 透传错误,产物**不落盘** |

---

## 自查规则(用于 §5 Review Gate)

| 项 | 阈值 | 提示文案 |
|---|---|---|
| Open Questions 总数 | > 0 | "N 个 open question 未解决 —— 强烈建议先回答再 handoff" |
| Cross-Product Conflicts 数 | > 0 | "N 个跨产物冲突 —— 见 open-questions.md ## Cross-Product Conflicts" |
| Verification Checklist 未勾选项 | > 0 | "Verification checklist 有 N 项待勾选" |
| INFERRED 标记数 | > 0 | "spec 中有 N 处 INFERRED 内容,建议人工核对" |

---

## SKILL.md 必须包含的章节(交付清单)

1. Frontmatter — `name: figma-emit-spec`,`description: ...`
2. Position in figma-workflow — 阶段 E + handoff 出口
3. Prerequisites — 5 份上游产物
4. Calling convention
5. 产物格式 — `implementation-spec.md` + `open-questions.md` 两份
6. Cross-product conflicts 检测规则
7. Handoff 出口规则(指向 §5)
8. Self-check rules
9. Downstream — 用户的 coding agent / `superpowers:writing-plans` / 自己实现
