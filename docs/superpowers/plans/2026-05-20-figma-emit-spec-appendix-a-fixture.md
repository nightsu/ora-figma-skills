# Plan P3 — Appendix A: Fixture Content for `referral-home`

> Companion of: [2026-05-20-figma-emit-spec.md](./2026-05-20-figma-emit-spec.md)
>
> 本附录提供 Task 7 中要写入 `figma-emit-spec/tests/fixtures/referral-home/` 下各文件的**完整内容**。
> 沿用 P2 referral-home 主题保持套件 fixture 数据连贯;故意在 `component-mapping.md` 中注入
> label_drift(C2 label 与 D label 不一致)+ 在 `clarified-requirement.md` 注入 `[deferred]` 标记,
> 展示 emit-spec 的两个核心机制工作。

---

## A1. `tests/fixtures/referral-home/README.md`

```markdown
# Fixture: referral-home

回归 fixture,用于验证 `figma-emit-spec` 的合成行为。

**来源:** 沿用 P2 `figma-design-token/tests/fixtures/referral-home/` 主题,
扩展为 phase A~D 五份模拟产物 + 期望 phase E 输出。

## 用途

人工 review 验证。让 Agent 读 `inputs/` 下的 5 份模拟产物,合成 `implementation-spec.md` + `open-questions.md`,
和 `expected/` 对比,确认**关键行为**:
- `label_drift` 自动校正(C2 槽位 label 被 D 真实 label 覆盖)
- `[deferred]` 标记在 spec 中显式标"本期不实现"
- 跨产物冲突写入 open-questions(field_unbound / module_missing_token / module_drift)
- 6 phase 分段汇总 open questions

## 注入的测试场景

- `component-mapping.md` 中 `DiamondPreviewCard.primary_value` label 故意写成"钻石余额"(C2 推测)
  → `design-token-patch.md` 中真实 label 是"钻石数量"(D 真值)
  → emit-spec 应自动校正,implementation-spec 用"钻石数量",并在 Auto Corrections Metadata 段说明
- `clarified-requirement.md` 第 1 个 open question 带 `[deferred]` 前缀
  → emit-spec 应在对应 Module 段显式标 Deferred + 在 open-questions.md Deferred 段汇总

## 文件

- `inputs/clarified-requirement.md` — phase A 模拟产物(含 [deferred] open question)
- `inputs/ui-understanding.md` — phase B 模拟产物
- `inputs/api-mapping.md` — phase C1 模拟产物
- `inputs/component-mapping.md` — phase C2 模拟产物(故意含 label_drift)
- `inputs/design-token-patch.md` — phase D 模拟产物(直接复用 P2 fixture)
- `expected/implementation-spec.md` — 期望主产物
- `expected/open-questions.md` — 期望 open-questions 汇总

## MVP 验证方式

人工 review。Agent 跑完后,用 `diff` 对比产物 vs expected:
- 关键看 Auto Corrections Metadata 段 + Module 段的 Deferred 标记
- 关键看 open-questions 的分段结构 + Deferred 段
- 允许格式略有浮动(空行 / 列序),不允许语义偏离

## 未来

第 3 版引入 LLM-as-judge 或结构化等价检查。
```

---

## A2. `tests/fixtures/referral-home/inputs/clarified-requirement.md`

```markdown
# Clarified Requirement — referral-home

## Goal
实现转介绍首页,展示用户钻石余额、运营入口、banner 轮播、热门商品列表。

## Scope
- 钻石卡片(余额 + 记录入口)
- 运营入口网格(4 个入口)
- Banner 轮播
- 热门商品列表

## Out of Scope
- 钻石记录详情页(本期占位跳转)
- 商品详情页(本期占位跳转)

## User States
- loading / empty / error / success

## Interaction
- 点击钻石记录 → 跳转钻石记录页
- 点击金刚位 → 根据 jumpType 跳转
- 点击商品 → 打开商品详情或兑吧 H5

## Constraints
- 使用项目现有 React + TypeScript 栈
- 移动端优先

## Open Questions
- [ ] [deferred] 趋势数据(钻石余额右侧 ↑/↓ X%):接口完全无字段,本期不实现 trend badge
- [ ] 入口数量是否固定 4 个?
- [ ] 商品列表是否分页?
```

---

## A3. `tests/fixtures/referral-home/inputs/ui-understanding.md`

```markdown
# UI Understanding — referral-home

## Page Structure

- ReferralHomePage
  - DiamondPreviewCard
  - OperationEntryGrid (4 instances)
  - BannerCarousel
  - HotProductList (待 phase C2 确认是否在本视图内)

## Suspected Components

| UI Area | Meaning | Candidate Component | Confidence |
|---|---|---|---|
| 顶部钻石卡片 | 钻石余额 | DiamondPreviewCard | high |
| 4 个运营入口 | 入口网格 | OperationEntryGrid | high |
| Banner | 轮播图 | BannerCarousel | high |
| 商品列表 | 热门商品 | HotProductList | medium |

## Visual Notes
- 整体移动端布局,375 设计宽度
- 卡片式容器,圆角 12px

## Non-business UI To Ignore
- StatusBar / HomeIndicator
- DeviceFrame
- 装饰背景

## Questions
- HotProductList 是否真的在本页(MVP 第 1 屏)?可能需 phase C2 核对 Figma node 确认
```

---

## A4. `tests/fixtures/referral-home/inputs/api-mapping.md`

```markdown
# API Mapping — referral-home

## Data Sources

| UI Module | API | Method | Description |
|---|---|---|---|
| DiamondPreviewCard | /api/diamond/overview | GET | 钻石概览 |
| OperationEntryGrid | /api/operation/entries | GET | 运营入口列表 |
| BannerCarousel | /api/banner/list | GET | banner 列表 |

## Field Mapping

| UI Field | API Field | Type | Transform |
|---|---|---|---|
| 钻石数量 | user.diamond.balance | number | formatNumber |
| 运营入口 icon | entries[].iconUrl | string | CDN URL |
| 运营入口 title | entries[].title | string | none |
| banner 图 | banners[].imageUrl | string | CDN URL |

## State Mapping

| State | Trigger | UI Behavior |
|---|---|---|
| loading | request pending | 骨架屏 |
| empty | list.length === 0 | 空状态 |
| error | request failed | 错误 toast |
```

---

## A5. `tests/fixtures/referral-home/inputs/component-mapping.md`

> ⚠ **故意注入 label_drift**: `DiamondPreviewCard.primary_value` label 写成"钻石余额",
> 但 `design-token-patch.md`(A6)中真实 label 是"钻石数量"。emit-spec 应自动校正。

```markdown
# Component Mapping — referral-home

> Generated by figma-ui-api-mapper@0.4.0 at 2026-05-20T15:08:33+08:00
> Source: figma file=FixtureReferral0001 node=200:100 (ReferralHomePage)

## Summary

- api_bound: 6
- ui_static: 3
- ui_copy: 2
- unknown: 0
- needs_confirmation: 0

## Modules

### DiamondPreviewCard
| node_id | binding_type | ui_role | api_field | confidence | needs_confirmation | reason |
|---|---|---|---|---|---|---|
| diamond_card_bg | ui_static | container | — | 0.95 | false | — |
| diamond_label | ui_copy | label "钻石" | — | 0.92 | false | — |
| diamond_value | api_bound | primary_value "钻石余额" | user.diamond.balance | 0.96 | false | C2 推测 label(可能错位) |
| record_entry_icon | ui_static | entry_icon | — | 0.95 | false | — |
| record_entry_text | ui_copy | entry_text "记录" | — | 0.88 | false | — |

### OperationEntryGrid
| node_id | binding_type | ui_role | api_field | confidence | needs_confirmation | reason | repeat_group |
|---|---|---|---|---|---|---|---|
| entry_icon | api_bound | list_item_icon | entries[].iconUrl | 0.94 | false | — | entry_grid_item |
| entry_label | api_bound | list_item_label "入口" | entries[].title | 0.95 | false | C2 推测 label(可能错位) | entry_grid_item |

### BannerCarousel
| node_id | binding_type | ui_role | api_field | confidence | needs_confirmation | reason | repeat_group |
|---|---|---|---|---|---|---|---|
| banner_image | api_bound | list_item_image | banners[].imageUrl | 0.96 | false | — | banner_item |
| banner_indicator | ui_static | carousel_indicator | — | 0.92 | false | — | — |

## Open Questions
- node `hot_product_section`: 不确定本页是否包含 HotProductList,需 phase D 视觉确认

## Ignored Nodes
- StatusBar / HomeIndicator / DeviceFrame / 装饰背景
```

---

## A6. `tests/fixtures/referral-home/inputs/design-token-patch.md`

> 直接复用 P2 `figma-design-token/tests/fixtures/referral-home/expected/design-token-patch.md` 内容,
> 故 label 是 D 真值(钻石数量 / 运营入口 等)。

```markdown
# Design Token Patch — referral-home

> Generated by figma-design-token@0.1.0 at 2026-05-20T15:42:01+08:00
> Source: figma file=FixtureReferral0001 node=200:100 (ReferralHomePage)

## Page Metrics
| Item | Value |
|---|---|
| Design width | 375px |
| Page padding | 12px |
| Section gap | 16px |

## Module Tokens

### DiamondPreviewCard
| Token | Value | Source |
|---|---|---|
| borderRadius | 12px | direct |
| padding | 12px 16px | variable: spacing/card/padding |
| background | #FFFFFF | direct |
| titleFontSize | 14px | direct |
| valueFontSize | 24px | direct |
| valueFontWeight | Semi Bold | direct |
| valueColor | #FF6A3D | variable: color/brand/primary |
| valueLabel | 钻石数量 | direct (真实 characters) |

### OperationEntryGrid
| Token | Value | Source |
|---|---|---|
| columnCount | 4 | INFERRED (from 4-instance grid) |
| itemGap | 8px | direct |
| iconSize | 32px | direct |
| labelFontSize | 12px | direct |
| labelText | 运营入口 | direct (真实 characters, 列表项模板 label) |

### BannerCarousel
| Token | Value | Source |
|---|---|---|
| imageHeight | 120px | direct |
| indicatorSize | 6px | direct |

## Variables (from Figma variables)
| Name | Value | Used by |
|---|---|---|
| color/brand/primary | #FF6A3D | DiamondPreviewCard.valueColor |
| spacing/card/padding | 12px | DiamondPreviewCard.padding |

## Assets
| Asset | Node ID | Usage | Suggested Export |
|---|---|---|---|
| banner_image | 123:789 | BannerCarousel | png |
| icon_diamond | 123:790 | DiamondPreviewCard | svg |
| icon_arrow | 123:791 | DiamondPreviewCard | svg |

## Ignored
- StatusBar / HomeIndicator / 装饰背景
```

---

## A7. `tests/fixtures/referral-home/expected/implementation-spec.md`

```markdown
# Implementation Spec — referral-home

> Source of truth for Agent apply stage. DO NOT consume raw Figma JSON in apply.
> Generated by figma-emit-spec@0.1.0 at 2026-05-20T17:00:00+08:00
> Label drift auto-corrections: 2(详见 Auto Corrections Metadata 段)

## Page
ReferralHomePage

## Goal
实现转介绍首页,展示用户钻石余额、运营入口、banner 轮播、热门商品列表。

## Scope / Out of Scope

**Scope:**
- 钻石卡片(余额 + 记录入口)
- 运营入口网格(4 个入口)
- Banner 轮播
- 热门商品列表

**Out of Scope:**
- 钻石记录详情页(本期占位跳转)
- 商品详情页(本期占位跳转)

## Modules

### 1. DiamondPreviewCard

**Responsibilities** (INFERRED 从 ui-understanding + clarified-requirement 推断)
- 展示用户当前钻石余额
- 提供钻石记录入口

**Data Binding** (从 api-mapping + component-mapping 合并)
| UI Field | API Field | Transform |
|---|---|---|
| 钻石数量 | user.diamond.balance | formatNumber |
| 记录入口 | — (固定文案) | — |

**Component** (从 component-mapping 推断)
- 优先复用项目已有 `DiamondPreviewCard`(若存在)
- 否则新建 `components/DiamondPreviewCard/`

**Design Tokens** (从 design-token-patch 抽取)
- borderRadius: 12px
- padding: 12px 16px (variable: spacing/card/padding)
- background: #FFFFFF
- titleFontSize: 14px
- valueFontSize: 24px
- valueFontWeight: Semi Bold
- valueColor: #FF6A3D (variable: color/brand/primary)
- valueLabel: 钻石数量 (D 真值,已自动校正)

**States**
- loading / empty / error / success

#### DiamondPreviewCard - 趋势 Badge

> ⚠ Deferred: 趋势数据(钻石余额右侧 ↑/↓ X%):接口完全无字段,本期不实现 trend badge
> 来源:phase A open-questions.md #1

(本元素不参与本期实施)

### 2. OperationEntryGrid

**Responsibilities** (INFERRED)
- 展示 4 个运营入口
- 点击根据 jumpType 跳转

**Data Binding**
| UI Field | API Field | Transform |
|---|---|---|
| 运营入口 icon | entries[].iconUrl | CDN URL |
| 运营入口 title | entries[].title | none |

**Component**
- 优先复用项目已有 `OperationEntryGrid`(若存在)
- 否则新建 `components/OperationEntryGrid/`

**Design Tokens**
- columnCount: 4 (INFERRED from 4-instance grid)
- itemGap: 8px
- iconSize: 32px
- labelFontSize: 12px

**States**
- loading / empty / error

### 3. BannerCarousel

**Responsibilities** (INFERRED)
- 展示运营 banner 轮播

**Data Binding**
| UI Field | API Field | Transform |
|---|---|---|
| banner 图 | banners[].imageUrl | CDN URL |

**Component**
- 优先复用项目已有 `BannerCarousel`(若存在)
- 否则新建 `components/BannerCarousel/`

**Design Tokens**
- imageHeight: 120px
- indicatorSize: 6px

**States**
- loading / empty / error

## API Usage
| Module | API | Suggested Hook |
|---|---|---|
| DiamondPreviewCard | GET /api/diamond/overview | useDiamondOverview |
| OperationEntryGrid | GET /api/operation/entries | useOperationEntries |
| BannerCarousel | GET /api/banner/list | useBannerList |

## Interaction
- 点击钻石记录 → 跳转钻石记录页
- 点击金刚位 → 根据 jumpType 跳转
- 点击商品 → 打开商品详情或兑吧 H5

## Implementation Constraints
- 不直接复刻 Figma 图层层级
- 不将装饰性节点实现为独立组件
- 重复列表项使用数据驱动渲染
- 优先复用项目已有组件 / tailwind / css_module
- 样式 token 来自 design-token-patch.md
- API 字段以 api-mapping.md 为准
- 使用项目现有 React + TypeScript 栈
- 移动端优先

## Verification Checklist
- [ ] 与 Figma 截图视觉对比(允许文本不严格一致)
- [ ] 接口字段类型与 API 文档一致
- [ ] 复用项目已有组件 / 框架
- [ ] 所有 open questions 已解决或显式 defer
- [ ] 所有 deferred items 已确认本期不实现

## Open Questions
见 open-questions.md

## Auto Corrections Metadata
- label_drift: 2 个 label 已从 phase C2 自动校正为 phase D 真实值
- 校正详情:
  - DiamondPreviewCard.primary_value: "钻石余额"(C2 推测) → "钻石数量"(D 真值)
  - OperationEntryGrid.list_item_label: "入口"(C2 推测) → "运营入口"(D 真值)
```

---

## A8. `tests/fixtures/referral-home/expected/open-questions.md`

```markdown
# Open Questions — referral-home

聚合自所有阶段产物中标记的待确认项。

## From Phase A (clarified-requirement.md)
- [ ] 入口数量是否固定 4 个?
- [ ] 商品列表是否分页?

## From Phase B (ui-understanding.md)
- [ ] HotProductList 是否真的在本页(MVP 第 1 屏)?可能需 phase C2 核对 Figma node 确认

## From Phase C1 (api-mapping.md)
(无)

## From Phase C2 (component-mapping.md, needs_confirmation: true)
- [ ] node `hot_product_section`: 不确定本页是否包含 HotProductList,需 phase D 视觉确认

## From Phase D (design-token-patch.md, INFERRED)
- [ ] OperationEntryGrid.columnCount: INFERRED from 4-instance grid,需确认是否固定

## Cross-Product Conflicts (auto-detected)
- [ ] field_unbound: (本 fixture 无此类冲突,留作示例段)
- [ ] module_drift: HotProductList(ui-understanding 出现,component-mapping 未确认)

(label_drift 已自动校正,不出现在此处 —— 详见 implementation-spec.md ## Auto Corrections Metadata)

## Deferred (auto-detected from [deferred] prefix)
- [ ] [Phase A] 趋势数据(钻石余额右侧 ↑/↓ X%):接口完全无字段,本期不实现 trend badge
```
