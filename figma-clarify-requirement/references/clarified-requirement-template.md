# Clarified Requirement — <feature>

> Generated by figma-clarify-requirement@<version> at <ISO8601>

## Goal

用 1-3 句话说明本页面或功能要解决的问题。只能写用户已确认的目标;如由 agent 推断,必须标记 `(INFERRED)`。

## Scope

- 列出本期必须实现的范围。
- 每条范围应可被后续 phase B/C/D/E 继续消费。

## Out of Scope

- 列出本期明确不做的内容。
- 如果是不阻塞本期但后续可能要做的事项,可同时在 `Open Questions` 使用 `[deferred]` 标记。

## User States

- loading:
- success:
- empty:
- error:
- no-permission:

只保留本期需要覆盖的状态;暂不覆盖的状态写明 `[deferred]`。

## Interaction

- 列出关键交互、跳转、hover/click 行为、埋点或无交互说明。

## Constraints

- 技术栈:
- 复用要求:
- 兼容性:
- 实施边界:

## Open Questions

- [ ] [deferred] 示例: 本期不实现但需要后续确认的问题。
- [ ] 示例: 进入下一阶段前需要用户、设计或后端确认的问题。

## 填写约定

- `Open Questions` items may use `[deferred]` when explicitly out of scope for this implementation.
- Empty `Goal` or `Scope` means the product is not ready for Phase B.
- Facts inferred by the agent must be marked `(INFERRED)`.
- 不要保留 `<feature>`、`<version>`、`<ISO8601>` 这类模板占位。
